import fileinput

class Input:

    def main(self):
        input_int = int(raw_input()) # Converte para inteiro
        input_float = float(raw_input()) # Converte para ponto flutuante
        input_str = str(raw_input()) # Converte para string
        input_tuple = tuple(raw_input()) # Converte para tuple
